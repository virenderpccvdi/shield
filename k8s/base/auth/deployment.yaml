apiVersion: apps/v1
kind: Deployment
metadata:
  name: shield-auth
  labels:
    app: shield-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shield-auth
  template:
    metadata:
      labels:
        app: shield-auth
    spec:
      containers:
        - name: shield-auth
          image: REGISTRY/shield-auth:TAG
          ports:
            - containerPort: 8281
          envFrom:
            - configMapRef:
                name: shield-config
            - secretRef:
                name: shield-secrets
          env:
            - name: SERVER_PORT
              value: "8281"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://shield-eureka:8261/eureka/"
            - name: EUREKA_INSTANCE_PREFERIPADDRESS
              value: "true"
            - name: SPRING_CONFIG_IMPORT
              value: "optional:"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "300m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8281
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8281
            initialDelaySeconds: 90
            periodSeconds: 20
            failureThreshold: 3
