version: 2

updates:
  # ── Maven (all Java microservices) ─────────────────────────────────────────
  - package-ecosystem: "maven"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "java"
    commit-message:
      prefix: "chore(deps)"
    ignore:
      # stripe-java 32.x removed Invoice.getSubscription/getPaymentIntent + InvoiceLineItem.getPrice
      # WebhookProcessingService must be migrated manually before allowing a major upgrade
      - dependency-name: "com.stripe:stripe-java"
        update-types: ["version-update:semver-major"]
    groups:
      spring-boot:
        patterns:
          - "org.springframework.boot:*"
      spring-cloud:
        patterns:
          - "org.springframework.cloud:*"
      security:
        patterns:
          - "io.jsonwebtoken:*"
          - "org.springframework.security:*"

  # ── npm (React dashboard) ──────────────────────────────────────────────────
  - package-ecosystem: "npm"
    directory: "/shield-dashboard"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "javascript"
    commit-message:
      prefix: "chore(deps)"
    groups:
      react:
        patterns:
          - "react"
          - "react-dom"
          - "react-router-dom"
          - "@types/react*"
      mui:
        patterns:
          - "@mui/*"
          - "@emotion/*"
      vite-toolchain:
        patterns:
          - "vite"
          - "@vitejs/*"
          - "typescript"
    ignore:
      # Only patch/minor updates for major UI libraries — avoid breaking changes
      - dependency-name: "react"
        update-types: ["version-update:semver-major"]
      # All MUI packages must be upgraded together (peer deps must share major version).
      # Block individual major bumps; do a coordinated MUI v7→v8 migration PR manually.
      - dependency-name: "@mui/material"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@mui/icons-material"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@mui/x-date-pickers"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@emotion/react"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@emotion/styled"
        update-types: ["version-update:semver-major"]
      # TypeScript 6 and Vite 8 require dedicated migration — block major auto-bumps.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "vite"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@vitejs/plugin-react"
        update-types: ["version-update:semver-major"]
      # recharts 2→3 has breaking chart API changes; 18+ files need verification.
      - dependency-name: "recharts"
        update-types: ["version-update:semver-major"]

  # ── pip (shield-ai Python service) ─────────────────────────────────────────
  - package-ecosystem: "pip"
    directory: "/shield-ai"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "python"
    commit-message:
      prefix: "chore(deps)"
    # python-jose removed entirely (CVE-2024-33663, unmaintained) — no active ignores needed

  # ── Flutter (mobile app) ───────────────────────────────────────────────────
  - package-ecosystem: "pub"
    directory: "/shield-app"
    schedule:
      interval: "weekly"
      day: "tuesday"
      time: "09:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "flutter"
    commit-message:
      prefix: "chore(deps)"
    ignore:
      # Only minor/patch for Firebase — major versions require manual migration
      - dependency-name: "firebase_core"
        update-types: ["version-update:semver-major"]
      - dependency-name: "firebase_messaging"
        update-types: ["version-update:semver-major"]
      # flutter_riverpod 2→3: StateNotifier→Notifier/AsyncNotifier migration; 245+ usages.
      - dependency-name: "flutter_riverpod"
        update-types: ["version-update:semver-major"]
      - dependency-name: "riverpod"
        update-types: ["version-update:semver-major"]
      - dependency-name: "hooks_riverpod"
        update-types: ["version-update:semver-major"]
      # go_router: 3-major-version jump (14→17); 88+ route usages need manual migration.
      - dependency-name: "go_router"
        update-types: ["version-update:semver-major"]

  # ── GitHub Actions ─────────────────────────────────────────────────────────
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      time: "09:00"
      timezone: "Asia/Kolkata"
    labels:
      - "dependencies"
      - "ci"
    commit-message:
      prefix: "chore(ci)"
