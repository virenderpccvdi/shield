# Shield Dashboard environment variables
# Copy to .env.local and adjust as needed.
# Leave VITE_API_URL empty to use same-origin requests (recommended for production behind nginx).
VITE_API_URL=https://shield.rstglobal.in
